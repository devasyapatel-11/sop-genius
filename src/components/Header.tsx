const Header = () => (
  <header className="sticky top-0 z-50 border-b border-border backdrop-blur-sm bg-background/80 no-print">
    <div className="max-w-content mx-auto flex items-center justify-between px-6 py-4">
      <span className="text-xl font-semibold text-primary">SOPwise</span>
      <span className="text-xs font-medium px-3 py-1 rounded-full border border-primary/30 text-primary bg-primary/10">
        AI-Powered
      </span>
    </div>
  </header>
);

export default Header;
